# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Interactive Computing Education Platform — a static web hub serving self-contained HTML-based CS activities for UK secondary school students (Years 8, 9, 11). Zero dependencies, no build step, no package manager.

## Architecture

- **`index.html`** — Hub page that fetches `activities.json` and `meta.json` to dynamically render activity cards grouped by year and topic
- **`Y8/`, `Y9/`, `Y11/`, `Demo/`** — Year group folders containing topic subfolders with standalone activity HTML files
- **`activities.json`** and **`meta.json`** — Auto-generated by GitHub Actions workflow on push to main; never edit manually
- **`.github/workflows/build-manifest.yml`** — Parses `<!-- ACTIVITY -->` comment blocks from HTML files to rebuild the manifest files
- **`shared.css`** — Shared reset, year-group colour tokens, topic accent overrides, and `ac-*` utility classes. Used by new activities built from templates; existing activities are not required to adopt it.
- **`classroom.js`** — Shared Google Classroom integration. Referenced by activities via `../../classroom.js`.
- **`_templates/`** — Activity starter templates (excluded from manifest). Copy `Y8.html`, `Y9.html`, or `Y11.html` as the basis for a new activity.

## Running Locally

Open `index.html` in a browser. No server or build step required. Each activity HTML file can also be opened directly.

## Adding a New Activity

1. Copy the appropriate template from `_templates/` into the correct folder: `Y<number>/<Topic>/filename.html`
2. Update the `<!-- ACTIVITY -->` metadata block at the top:
   ```html
   <!-- ACTIVITY
   title:  Activity Title
   type:   Practice or Challenge
   stars:  1-4
   desc:   Short description for the hub card
   color:  teal|amber|red|green|blue|cyan|purple|indigo
   -->
   ```
3. Set `data-year` and `data-topic` on `<body>` to apply the correct palette from `shared.css`
4. Replace placeholder content with your tasks; update `TASK_COUNT` in the script to match
5. Push to main — the workflow auto-generates `activities.json` and `meta.json`

## Shared Files

Two files are intentionally shared across activities via relative paths (`../../`):

| File | Purpose | Used by |
|------|---------|---------|
| `shared.css` | CSS reset, year-group themes (`data-year`), topic accent overrides (`data-topic`), `ac-*` utility classes | New activities (from templates) |
| `classroom.js` | Google Classroom sign-in, grade submission, teacher mode | Any activity that includes it |

These files are allowed exceptions to the self-contained rule because:
- Activities are always accessed through the hub or GitHub Pages, never as isolated downloads
- Both files degrade gracefully: a missing `classroom.js` just disables Classroom features; activities built from templates rely on `shared.css` being co-located in the repo

**Do not reference any other external files** from activity HTML — no CDN URLs, no third-party libraries.

## Year-Group Themes (shared.css)

Apply themes by setting attributes on `<body>`:
```html
<body data-year="8"  data-topic="Python">          <!-- light navy/teal, system fonts -->
<body data-year="9"  data-topic="Hardware_Networks"> <!-- dark space, DM Sans + Space Mono -->
<body data-year="11" data-topic="Networks">          <!-- dark navy, system fonts -->
```

Topic values map to accent colour overrides. Current supported topics:

| Year | Topics |
|------|--------|
| 8    | Python |
| 9    | Hardware_Networks, Algorithms, Cybersecurity, Databases |
| 11   | Networks, NetworkHardware, Legislation, System_Software, Algorithms, Security |

To add a new topic, append a `[data-year="…"][data-topic="…"]` rule to `shared.css`.

## ac-* Utility Classes (shared.css)

New activities can use these instead of re-implementing them inline:

| Class | Purpose |
|-------|---------|
| `.ac-btn` | Primary action button (uses `--accent`) |
| `.ac-btn-secondary` | Ghost/outline button |
| `.ac-banner` | Fixed top completion banner; add `.show.gold` or `.show.green` |
| `.ac-toast` | Fixed bottom-right toast; add `.show` + optional `.warn`/`.error` |
| `.ac-progress-bar` + `.ac-progress-fill` | Animated progress bar |
| `.ac-code` | Code block (uses `--code-bg` / `--code-text`) |
| `.sr-only` | Screen-reader-only text |

Animations available: `ac-shake`, `ac-fade-in`, `ac-slide-down`, `ac-pulse-accent`, `ac-pulse-gold`.

## Key Conventions

- **Self-contained logic**: Every activity's HTML structure, questions, and JS remain inline — no external game logic
- **Shared presentation only**: `shared.css` and `classroom.js` are the only permitted relative-path dependencies
- **Vanilla JS only**: No frameworks or libraries; direct DOM manipulation
- **Files/folders starting with `_`** are excluded from the manifest (use `_drafts/` for WIP, `_templates/` for starters)
- **Folder structure determines metadata**: `Y8/` → "Year 8", folder names become topic labels
- **Color themes**: The `color:` field in the `<!-- ACTIVITY -->` block controls card styling in the hub; `data-topic` on `<body>` controls the activity's own accent colour
